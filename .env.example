# Single User Authentication
USER_USERNAME=demo
USER_PASSWORD=Mem0ryN0te#2026!Secure

# JWT Secret (generate a random 64-char string)
JWT_SECRET=replace_with_your_own_random_secret_string

# Server Port
PORT=3000

# Database (LanceDB)
# Uses LanceDB for unified storage with built-in vector search
DB_PATH=./data/notebookme.lance

# AI Configuration (OpenAI Compatible API)
VITE_AI_API_URL=https://api.z.ai/api/coding/paas/v4
VITE_AI_API_KEY=your_openai_compatible_api_key_here
VITE_AI_MODEL_NAME=GLM-4.7

# Migration (only needed if migrating from SQLite)
# OLD_DB_PATH=./data/memorynote.db
# NEW_DB_PATH=./data/notebookme.lance
